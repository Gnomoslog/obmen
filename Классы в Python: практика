import datetime

class Quest:
    def __init__(self, name, description, goal):
        self.name = name
        self.description = description
        self.goal = goal
        self.start_time = None
        self.end_time = None
        print(f'новое приключение {name} началось')

    def go(self):
        if self.end_time is not None:
            return 'С этим испытанием вы уже справились'
        self.start_time = datetime.datetime.now()
        return f'Начало {self.name} положено'

    def ad(self):
        if self.start_time is None:
            return 'Нельзя завершить то, что не имеет начала!'
        self.end_time = datetime.datetime.now()
        ma = self.end_time - self.start_time
        return f'Квест {self.name} окончен. Время выполнения квеста: {ma}'

    def __str__(self):
        if self.end_time is not None:
            return f'Цель квеста {self.name} — {self.goal}. Квест завершён.'
        elif self.start_time is not None:
            return f'Цель квеста {self.name} — {self.goal}. Квест выполняется.'
        else:
            return f'Цель квеста {self.name} — {self.goal}.'

ada = " безграничная лужа "
ad = " мне лень объяснять этот квест но у меня есть девочка зайка 😎👌🔥"
addd = "_we_shold_pay_rent_alredy_three_mounts_ago_but_we_forgot"

new_st = Quest(ada, ad, addd)

print(new_st)
print(new_st.go())
print(new_st)
print(new_st.ad())
print(new_st)
